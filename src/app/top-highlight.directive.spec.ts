import { TopHighlightDirective } from './top-highlight.directive';

describe('TopHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new TopHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
