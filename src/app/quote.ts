export class Quotes {

    constructor( public index:number, public author: string, public quote:string, public submitter: string, public datePost: Date){

    }
    // author: string;
    // quote: string;
}
